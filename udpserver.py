#!usr/bin/python

#import socket

#msgtoclient = str.encode("Salut Client UDP")

#sock = socket.socket(TODO,TODO)

#host = TODO
#port = TODO

#sock.bind((host,port))
#print("En attente d'un message du client...")
#while True:

	#msgClient = sock.recvfrom(1024)
	#message = msgClient[0]
	#addr = msgClient[1]
	#clientMsg = "Messages reçus: {}".format(message)
	#clientIP = "Adresse IP du client: {}".format(addr)
	#print (clientMsg)
	#print(clientIP)

	#sock.sendto(msgtoclient,addr)
