#!/usr/bin/env python3

#import socket

#HOST = TODO
#PORT = TODO

#with socket.socket(TODO, TODO) as s:
    #s.bind((HOST, PORT))
    #s.listen()
    #conn, addr = s.accept()
    #with conn:
        #print('Connected by', addr)
        #while True:
            #data = conn.recv(1024)
            #if not data:
                #break
            #print("Message reçu: %s",data)
            #conn.sendall("J'ai bien reçu votre msg: "+data)
        #s.close()
